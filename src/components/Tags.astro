---
const { tags } = Astro.props;
---

<div class="flex flex-wrap gap-x-2 gap-y-1 mb-4">
  {
    tags.map((tag: string[]) => (
      <p class="text-sm text-white-1 transition duration-150 hover:text-orange-1">
        #{tag}
      </p>
    ))
  }
</div>
